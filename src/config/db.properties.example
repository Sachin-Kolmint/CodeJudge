# Copy this file to db.properties (same folder) and fill in your
# local MySQL credentials. db.properties is git-ignored so nobody
# accidentally commits real credentials.
db.url=jdbc:mysql://localhost:3306/codejudge?useSSL=false&serverTimezone=UTC
db.user=root
db.password=your_password_here
